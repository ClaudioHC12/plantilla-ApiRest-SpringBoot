spring.application.name=MainClass

server.port=8092

# Configuraci�n de la base de datos PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/Prueba
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuraci�n del dialecto de la base de datos (en este caso, para PostgreSQL)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuraci�n de la generaci�n de DDL autom�tico (si se desea)
spring.jpa.hibernate.ddl-auto=none

#Config openapi swagger
springdoc.swagger-ui.path=/swagger-ui-custom.html

#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration